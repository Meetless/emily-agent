# Extra deps for the local end-to-end demo harness (scripts/demo.py), which
# provisions a workspace and simulates human replies against a local Meetless
# checkout. Not needed to run the agent itself.
-r requirements.txt
psycopg2-binary>=2.9
